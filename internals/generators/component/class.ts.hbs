import * as React from 'react';
import {Wrap} from './styles';

type Props = {}
type State = {}

 export default class {{ properCase name }} extends {{{ type }}}<Props, State>{
  public readonly state: State = {};

  render(): JSX.Element {
    return (
      <Wrap>
        This is {{ properCase name }} component
      </Wrap>
    );
  }
}
