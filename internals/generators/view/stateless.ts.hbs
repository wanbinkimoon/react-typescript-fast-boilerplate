import * as React from 'react';

import {Wrap} from './styles';

const {{ properCase name }}: React.StatelessComponent<{}> = (props) => {
  return (
    <Wrap>
      This is {{ properCase name }} component
    </Wrap>
  );
};

export default {{ properCase name }};
